import java.util.Scanner

object Main extends App {
  val sc = new Scanner(System.in)
  var tmp = true
  var tmp2 = 1
  var count = 0
  while(tmp) {
    tmp2 = sc.nextInt()
    count += 1
    if(tmp2 == 0) {
      tmp = false
    } else {
      println(s"Case ${count}: ${tmp2}")
    }
  }
}
