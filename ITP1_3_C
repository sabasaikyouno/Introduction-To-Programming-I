import java.util.Scanner

object Main extends App {
  val sc = new Scanner(System.in)
  var tmp = true
  var a,b = 1
  while(tmp) {
    a = sc.nextInt()
    b = sc.nextInt()
    if(a == 0 && b == 0) {
      tmp = false
    } else {
      println(a.min(b)+" "+a.max(b))
    }
  }
}
